node_modules/

# Build output
dist/
out/
.next/
*.tsbuildinfo

# Credentials — the app stores real keys in the OS keychain, never here
.env
.env.*
!.env.example

.DS_Store
*.log

# Packaged output
release/

# Optional local-transcription stack: ~1.6 GB of binaries and model weights,
# fetched by `npm run setup:local`. The directory itself stays tracked.
electron/vendor/*
!electron/vendor/.gitkeep
